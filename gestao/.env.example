# Vazio = usa o mesmo host do frontend e o proxy do Vite (/api -> backend local).
# Isso permite testar pelo computador e por outro dispositivo na mesma rede sem apontar para o localhost do cliente.
VITE_API_URL=
VITE_PUBLIC_APP_URL=http://localhost:5174
