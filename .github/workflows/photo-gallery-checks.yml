name: Photo Gallery Checks

on:
  push:
    branches: [main]
    paths:
      - 'photo-gallery/**'
      - '.github/workflows/photo-gallery-checks.yml'
  pull_request:
    paths:
      - 'photo-gallery/**'
      - '.github/workflows/photo-gallery-checks.yml'

jobs:
  gallery:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: photo-gallery
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Typecheck
        run: npm run typecheck
      - name: Build
        run: npm run build
